# How to run
- Download the project
- Run `npm i ` to install dependcies
- Run `npm run start` and go to localhost:3000
- Make sure the [server](https://github.com/ababen1/weather_server) is also running at localhost:8000 !
